version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:  
    commands:
      - java -version
      - gradle build -warning-mode all

artifacts:
  type: zip
  files:
    - 'build/libs/*.jar'
